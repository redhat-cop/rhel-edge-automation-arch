---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "common.names.fullname" . }}-bootstrap
  namespace: {{ template "common.names.namespace" $ }}
  labels:
    {{- include "common.labels.labels" . | nindent 4 }}
data:
  bootstrap.sh: |
    {{- tpl (.Files.Get "files/bootstrap.sh" ) . | nindent 4 }}
