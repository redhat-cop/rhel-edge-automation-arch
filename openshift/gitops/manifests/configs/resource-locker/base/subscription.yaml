apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: resource-locker-subscription
  namespace: openshift-operators
spec:
  channel: alpha
  name: resource-locker-operator
  source: community-operators
  sourceNamespace: openshift-marketplace
